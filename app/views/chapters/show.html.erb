<div>
  <h1 class="font-bold text-4xl">Chapters#show</h1>
  <p>Find me in app/views/chapters/show.html.erb</p>
</div>

<div>
  <% @comments.each do |comment| %>
    <%= render CommentComponent.new(comment:) %>
  <% end %>
</div>
<div>
  <%= render CommentComponent.new(key: @chapter.id, key_type: "chapter") %>
</div>